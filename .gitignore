# Terraform state - never commit
terraform.tfstate
terraform.tfstate.backup
.terraform/terraform.tfstate

# Provider binaries - too large, reinstalled via terraform init
.terraform/providers/

# Sensitive variable files
*.tfvars
*.tfvars.json

# OS
.DS_Store
Thumbs.db
